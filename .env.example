# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=upgrade_upstox
POSTGRES_SERVER=db

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Cloudflare Tunnel Token
TUNNEL_TOKEN=your_tunnel_token_here

# Frontend Configuration
# When deploying, set this to your cloudflare domain, e.g., https://your-app.cloudflare.com
# For local docker-compose run, http://localhost:3000 is fine unless you want to access via tunnel
ORIGIN=http://localhost:3000
PUBLIC_API_URL=http://localhost:8000
